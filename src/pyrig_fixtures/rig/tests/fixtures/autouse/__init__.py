"""Autouse pytest fixtures organized by pytest scope.

Each submodule in this package contains autouse fixtures for a specific pytest
lifecycle stage. Fixtures run automatically without explicit use declarations
to enforce project structure, code quality, dependency health, and environment
standards across test runs.
"""
