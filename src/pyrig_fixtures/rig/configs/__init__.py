"""Declarative definitions of configuration files this package manages.

Mirrors pyrig's own config-file package path, so definitions placed here join
pyrig's discovery scope and are validated alongside its built-in config files
without any explicit registration.
"""
